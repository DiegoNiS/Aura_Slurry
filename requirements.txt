# Procesamiento de señal y modelo (Integrante 1)
librosa>=0.10
numpy>=1.24
soundfile>=0.12
# Fijado: modelo.joblib se serializó con esta versión exacta (los pickles de
# sklearn NO son compatibles entre versiones — si cambia, reentrenar)
scikit-learn==1.7.2
joblib>=1.3

# Backend (Integrante 2)
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
python-multipart>=0.0.9
google-genai>=0.2.0
python-dotenv>=1.0.0
